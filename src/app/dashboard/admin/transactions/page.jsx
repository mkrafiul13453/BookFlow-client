import React from 'react';

const adminTransactionPage = () => {
    return (
        <div>
            this is the admin transactions page
        </div>
    );
};

export default adminTransactionPage;